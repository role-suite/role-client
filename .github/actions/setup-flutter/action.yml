name: Setup Flutter
description: Install the stable Flutter SDK and fetch pub dependencies.

runs:
  using: composite
  steps:
    - uses: subosito/flutter-action@v2
      with:
        channel: stable
        cache: true

    - name: Install dependencies
      shell: bash
      run: flutter pub get
